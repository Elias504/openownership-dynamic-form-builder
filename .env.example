# =============================================================================
# Main stack environment — copy this file to .env and adjust as needed.
# Used by: docker compose up
# =============================================================================

# --- PostgreSQL -----------------------------------------------------------------
POSTGRES_DB=formbuilder
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# --- Ports (optional — these are the defaults) ----------------------------------
# DB_PORT=5432
# BACKEND_PORT=8081
# FRONTEND_PORT=3000
