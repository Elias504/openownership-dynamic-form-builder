---
extends: default

ignore: |
  node_modules/
  */node_modules/
  target/
  */target/

rules:
  document-start: disable
  line-length:
    max: 150
  truthy:
    allowed-values: ['true', 'false', 'on']
  comments:
    min-spaces-from-content: 1
  braces:
    forbid: false
